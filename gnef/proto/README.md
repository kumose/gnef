# NLP

files in this directory should be move to kumo api proto project.

原始 Query
↓
CharTokenizer（字符清洗）
↓
Normalize
↓
WordTokenizer（分词）
↓
SegmentResult（term + offset + idf）
↓
N-Gram
↓
SemanticTokenizer（BPE/SP）
↓
NER / Embedding