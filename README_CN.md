<!--
  Auto-generated by kmpkg tools
  --------------------------------
  This section/template was generated by kmpkg for reference.
  You may modify it freely to suit your project needs.
  Recommended to keep the structure for consistency across projects.
  version: 0.7.0
  date: 2026-03-23
-->

gnef
=============================

[English](./README.md)


# gnef 核心优势扩展总结

gnef 作为面向工业级场景的 C++ NLP 运行框架，核心价值在于打破传统 C++ NLP 开发“高性能与易用性不可兼得、算法与工程强耦合”的困境，以分层架构、插件化设计为核心，整合工程化能力与热更新特性，成为兼顾开发效率、运行性能与生产稳定性的一站式解决方案，以下从核心亮点展开详细扩展，总字数贴合1000字要求。

首先，四层接口的分层设计是 gnef 兼顾易用性与灵活性的核心支撑。框架严格遵循“上层不影响下层性能、下层不限制上层易用性”的原则，构建了从底层到顶层的完整接口体系。底层接口为纯 C++ 裸算子实现，无任何封装、虚函数或额外开销，聚焦分词、NER、文本标准化等核心能力的极致性能，可实现微秒级文本处理，满足高并发、低延迟的工业级需求；C++ 原生接口在底层算子基础上，做了类型安全、内存安全与线程安全的零开销封装，屏蔽了复杂的内存管理、词典加载等细节，为 C++ 开发者提供简洁、稳定的 API，大幅降低集成难度，提升开发效率；C++ 嵌入式 SQL 层是其创新亮点，README 中的 demo 可直观体现其便捷性，具体调用流程如下：

```shell
./build/gnef/gnef
Goose v0.6.10 (Galileo)
Enter ".help" for usage hints.
memory GNEF select nlp_process('我们来自清华大学，我们的导师是姚文君☺',4);
Invalid Error:
Must Initialize Gnef first! by call: pragma initialize_gnef_default; or pragma initialize_gnef('your dict config')
memory GNEF pragma initialize_gnef_default;
┌─────────┐
│ Success │
│ boolean │
├─────────┤
│ 0 rows │
└─────────┘
memory GNEF select nlp_process('我们来自清华大学，我们的导师是姚文君☺',4);
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ nlp_process('我们来自清华大学，我们的导师是姚文君☺', 4) │
│ varchar │
├─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ {"raw_query":"我们来自清华大学，我们的导师是姚文君☺","normalized":{"query":"我们来自清华大学我们的导师是姚文君"},"terms":{"terms":[{"main_term":{"term":"姚文君","pos":"x","offset":[{"offsets":[{"offset":45,"unicode_offset":3,"unicode_length":3}]}],"idf":11.739204307083542}},{"main_term":{"term":"导师","pos":"n","offset":[{"offsets":[{"offset":36,"unicode_offset":2,"unicode_length":2}]}],"idf":8.90346537821}},{"main_term":{"term":"清华大学","pos":"nt","offset":[{"offsets":[{"offset":12,"unicode_offset":4,"unicode_length":4}]}],"idf":8.08059472162}}]},"cost_us":36} │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

从启动 gnef 工具、初始化框架，到调用 nlp_process 函数处理文本、输出结构化结果，该 demo 直观体现了 SQL 接口的便捷性，业务方无需编写复杂 C++ 代码，直接适配业务系统；CLI 层则提供便捷的命令行交互，支持快速调试、测试 NLP 能力，无需编译部署，开箱即用，覆盖开发者、业务方、测试运维等全角色需求。

其次，框架与算法的彻底解耦，是 gnef 适配多业务场景的核心优势。gnef 清晰定位自身为“NLP 算法运行框架”，而非算法库，不负责分词准确率、NER 效果等算法层面的问题，仅专注于提供统一的运行环境、调度机制、标准化协议与工程化支撑。算法效果、业务规则、模型逻辑均通过插件形式接入框架，实现“框架管运行、插件管效果”的分离模式。这种设计意味着，更换业务场景时无需修改框架核心代码，仅需替换对应业务的算法插件；优化算法效果时，只需迭代插件中的词典、规则或模型，不影响框架的稳定性与运行效率，同时支持多插件共存，可满足多业务并行的需求，彻底解决了传统 C++ NLP 库算法与框架强耦合、换场景需重写、扩展成本高的痛点。

再者，极致的工程化能力，彻底解决了传统 C++ 项目的开发痛点。gnef 集成 kmpkg 依赖管理工具，通过配置文件即可实现第三方库的自动下载、编译、依赖解析与版本管理，无需手动维护复杂的 CMake 配置，仅需 2 行命令即可完成项目构建，大幅降低跨环境部署的难度，避免了依赖缺失、版本冲突导致的编译失败问题。同时，框架内置完善的单元测试与基准测试体系，测试代码与业务代码可一键编译，既能验证核心 API 的正确性，也能量化 NLP 能力的处理性能，为算法优化与生产部署提供数据支撑，让 C++ NLP 项目具备像 Python 项目一样的低门槛开发体验。

此外，高性能零损耗与天然的热更新能力，使其完全适配工业级生产场景。gnef 基于纯 C++ 原生开发，保留了 C++ 低延迟、高吞吐的核心优势，上层接口的封装几乎无性能损耗，底层算子的深度优化的实现了微秒级文本处理，可支撑高并发的 NLP 处理需求。同时，框架从架构层面支持热更新，词典、配置、算法插件均可热加载，通过简单的 SQL 命令即可完成在线更新，无需重启服务，实现无停机升级，有效避免了传统 C++ 项目更新需重启、影响业务连续性的问题，保障了生产环境的稳定性。

综上，gnef 以分层接口兼顾高性能与易用性，以插件化设计实现框架与算法的解耦，以完善的工程化能力降低开发与部署成本，以热更新特性保障生产稳定性，核心价值在于让业务方专注于具体场景的算法优化，无需投入精力解决工程化、性能、扩展性等底层问题。尤为值得关注的是，这种架构模式能高效支撑 agent 开发，结合 README 中的 demo，gnef 可作为 agent 的核心数据处理引擎：

```shell
memory GNEF pragma initialize_gnef_default;
memory GNEF select nlp_process('我们来自清华大学，我们的导师是姚文君☺',4);

```

agent 的感知模块获取文本数据后，可直接复用上述 demo 中的原生调用格式，通过 SQL 接口调用 nlp_process 函数，快速完成文本标准化、分词、NER 等处理，如 demo 中精准提取“清华大学”“姚文君”“导师”等关键信息，输出包含处理耗时、词性、IDF 值的结构化 JSON 结果，为 agent 决策提供精准的数据支撑；同时，插件化设计允许根据 agent 应用场景，灵活替换算法插件，优化分词、NER 逻辑，无需重构 agent 与 gnef 的对接代码，无需修改原生调用格式，即可快速提升 agent 数据分析与决策的精准度。它不是简单的 C++ NLP 工具包，而是一套真正面向生产、可扩展、可维护的工业级 NLP 运行框架，既彻底重构了 C++ NLP 的开发范式，也为 agent 的快速迭代、高效落地提供了坚实支撑，助力 agent 快速实现精准数据分析与智能决策。

## 🛠️ Build

本项目使用 [kmpkg](https://github.com/kumose/kmcmake) 进行依赖管理与构建集成。
kmpkg 会自动处理第三方库下载、依赖查找、编译标志配置等，避免手工维护复杂的 CMake 配置。

### 0. 准备环境

- Linux (Ubuntu 20.04+ / CentOS 7+ 推荐)
- CMake >= 3.25
- GCC >= 9.4 / Clang >= 12
- 已安装 `kmpkg`
  （参见 [安装文档](https://kumo-pub.github.io/docs/category/%E6%8C%81%E7%BB%AD%E9%9B%86%E6%88%90----kmpkg)）

### 1. 配置项目(可选)

- 完整的依赖请参见[`kmpkg.json`](kmpkg.json)
- 更新依赖基线请参见[`kmpkg-configuration.json`](kmpkg-configuration.json) 修改
  `default-registry`的`baseline`
- `baseline` 可通过 `git log` 获取最新提交
- 可选：用户可以自行管理依赖，确保 CMake 的 find_package 能正确找到所需库

    - 例如在系统路径或自定义路径安装依赖，并通过 CMAKE_PREFIX_PATH 指定
    - 或在 kmpkg 中声明外部依赖路径，避免重复下载

### 2. 编译项目

在项目根目录执行：

```bash
cmake --preset=defualt
cmake --build build -j$(nproc)
```

自管理依赖：

```shell
mkdir build
cd build
cmake ..
make -j$(nproc)
```

***注意***

    --preset=default 需确保已在项目根目录下定义相应 CMake Preset

### 3. 运行测试(可选)

在项目根目录执行：

```shell
ctest --test-dir build
```
